<%- include('./partials/head.ejs') %>

<main>
    <h1><a href="/" class="FDND">FDND</a></h1>
    <div class="grid-container-hobby">
        <h2 class="explanation"><%= squad.name %></h2>
        <h3 class="chosen-hobby">Gekozen hobby: <img src="/games.png" alt="gamen"></h3>
    </div>

    <!-- in de control flow hieronder wordt gecontroleerd
    of elke member uit de squad (forEach) een avatar heeft ingevuld. Als het antwoord hierop ja is,
    dan wordt er een img tag gegenereerd met daarin de avatar en een alt atribuut met diegene zijn naam.
    Maar als die niet is ingevuld, dan wordt er ook een img tag gegenereerd maar dan met als source een 
    placeholder plaatje. Elke keer als er een javascript moet worden uitgevoerd staat dit eromheen < > met procent tekens erin.-->
<div class="members-grid-container">  
    <% squad.members.forEach(member => { %> 
        <article> 
    <% if (member.avatar) { %> 

        <img src="<%= member.avatar %>" alt="De avatar van <%= member.name %>" > 

        <% }else{ %> 

        <img src="/placeholder.jpg" alt="Placeholder avatar" > 

        <% } %> 

        <h2><%= member.name %> <%= member.prefix %> <%= member.surname %></h2> 
        <a class="github-link" href="<%= member.gitHubHandle %>" target="_blank"><img src="/github-mark.png" alt="Github account van <%= member.name %>"></a>
        </article> 

        <% }) %> 
</div>
</main>

<%- include('./partials/foot.ejs') %>